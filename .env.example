# Provider configuration for MGX Librettist.
# Leave keys empty to run fully locally with mock providers.
PROVIDER_MODE=mock
MUSIXMATCH_API_KEY=
CYANITE_API_KEY=

# Optional, for future LLM-backed rewrites / briefs (not required in mock mode).
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
